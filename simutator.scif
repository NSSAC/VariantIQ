%appinstall simutator
cd /bioinf-tools
git clone https://github.com/iqbal-lab-org/simutator
cd simutator
pip install .

%apprun simutator
simutator $@

%apprun simutator_sciduct
mkdir -p $SCIF_APPDATA
#extract params from /job/job.json
#NAME="$(jq -r .input.name /job/job.json)"
#scif run simutator -- ....params...