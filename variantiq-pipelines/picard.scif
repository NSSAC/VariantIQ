%apprun picard
#we're running picard within the  picard environment here...
CMD="picard $@"
echo "Running . . . $CMD"
$CMD

%apprun varfier_sciduct
mkdir -p $SCIF_APPDATA
#extract params from /job/job.json
#NAME="$(jq -r .input.name /job/job.json)"
#scif run picard -- ....params...